<%= render "layouts/errors", object: @lesson %>

<%= form_for @lesson do |l| %>
<%= l.label :name %>
<%= l.text_field :name %>
<%= l.label :body %>
<%= l.text_field :body %>
<%= l.label :order_number %>
<%= l.text_field :order_number %>
<%= l.submit %>

<%= select_tag(:section_id, options_for_select(@section_option_array)) %>

<% end %>
