<% content_for(:title, @section.name + " | PROGRAMAJAMA") %>

<h1><%= @section.name %></h1>
<% @section.lessons.each do |lesson| %>
  <p>
<%= link_to lesson.name, lesson_path(lesson) %>
  </p>
<% end %>
